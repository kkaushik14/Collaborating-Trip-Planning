# Frontend

Vite + React (JSX) project with TailwindCSS configured.

## Commands

- `npm run dev` - start frontend dev server
- `npm run build` - build production assets
- `npm run preview` - preview production build locally
